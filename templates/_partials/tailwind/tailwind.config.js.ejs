/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx,vue}",
  ],
  theme: {
    extend: {},
  },
  plugins: [
<% if (ui === 'daisyui') { -%>
    require("daisyui"),
<% } -%>
  ],
<% if (ui === 'daisyui') { -%>
  daisyui: {
    themes: ["light", "dark", "cupcake", "synthwave"],
  },
<% } -%>
}
